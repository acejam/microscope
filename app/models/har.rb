class Har < ActiveRecord::Base
#	has_many :entry, :page

  # cell = Result.new(cell_name: filenames, icorrelation: intensities)
  # cell.save!
  # @har_file.pages.each do |page|
  #   Page.new(startedDateTime: page.started_date_time, idx: page.id, title: page.title, pageTimings: page.page_timings, comment: page.comment)
  # end
end
